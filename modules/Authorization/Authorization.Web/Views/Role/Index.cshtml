@model List<RoleDto>
@using Authorization.Contracts.Dtos
@using NetMX.Events
@{
    ViewData["Title"] = "Role Management";
}

<div class="hero is-primary">
    <div class="hero-body">
        <p class="title">Role Management</p>
        <p class="subtitle">Manage your roles</p>
    </div>
</div>

<div class="container mt-5">
    <div class="box">
        <div class="level mb-4">
            <div class="level-left">
                <div class="level-item">
                    <h2 class="title is-4">All Roles</h2>
                </div>
            </div>
            <div class="level-right">
                <div class="level-item">
                    <button class="button is-primary" 
                            hx-get="/Role/Create" 
                            hx-target="#form-container" 
                            hx-swap="innerHTML">
                        <span class="icon">
                            <i class="fas fa-plus"></i>
                        </span>
                        <span>Add Role</span>
                    </button>
                </div>
            </div>
        </div>

        <!-- Form Container (hidden initially) -->
        <div id="form-container" class="mb-4"></div>

        <!-- List Container -->
        <div id="list-container" 
             hx-get="/Role/List" 
             hx-trigger="load, @DomainEvents.Role.Created from:body, @DomainEvents.Role.Updated from:body">
            <div class="has-text-centered">
                <span class="icon is-large">
                    <i class="fas fa-spinner fa-pulse"></i>
                </span>
            </div>
        </div>
    </div>
</div>


<style>
    .htmx-indicator {
        display: none;
    }

    .htmx-request .htmx-indicator,
    .htmx-request.htmx-indicator {
        display: inline-block;
    }
</style>