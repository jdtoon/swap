@model LoginDto
@{
    Layout = "/Views/Shared/_LayoutNone.cshtml";
}

<div class="min-h-screen flex items-center justify-center p-4 bg-base-200">
    <div class="card w-full max-w-md bg-base-100 shadow-xl">
        <div class="card-body">
            <h2 class="card-title text-2xl font-bold mb-4 font-['Lumios_Marker']">App</h2>
            <div id="login-form">
                <partial name="_LoginForm" model="Model" />
            </div>
        </div>
    </div>
</div>
