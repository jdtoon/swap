@model PagedResult<RoomType>

<div class="overflow-x-auto">
    <div class="mb-4">
        <input type="text" 
               placeholder="Quick Search ..." 
               class="input input-bordered w-full max-w-xs"
               value="@ViewBag.CurrentSearch"
               hx-trigger="keyup changed delay:500ms"
               hx-get="@Url.Action("GetRoomTypeList")"
               hx-target="#roomTypeList"
               hx-on::after-settle="this.focus();"
               autofocus
               name="search" />
    </div>

    <table class="table w-full">
        <thead>
            <tr>
                <th>Room Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var roomType in Model.Data)
            {
                <tr id="roomType-@roomType.Id">
                    <td>@roomType.Name</td>
                    <td class="space-x-2">
                        <button class="btn btn-sm btn-info"
                                hx-get="@Url.Action("EditModal", new { id = roomType.Id })"
                                hx-target="#modalContainer">
                            Edit
                        </button>
                        <button class="btn btn-sm btn-error"
                                hx-delete="@Url.Action("Delete", new { id = roomType.Id })"
                                hx-target="#roomType-@roomType.Id"
                                hx-confirm="Are you sure you want to delete this room type?"
                                hx-swap="outerHTML">
                            Delete
                        </button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @if (Model.HasMore)
    {
        <div class="text-center mt-4">
            <button class="btn btn-ghost"
                    hx-get="@Url.Action("GetRoomTypeList", new { take = Model.CurrentPage + 10, search = ViewBag.CurrentSearch })"
                    hx-target="#roomTypeList"
                    hx-swap="innerHTML">
                Load More
            </button>
        </div>
    }
</div> 