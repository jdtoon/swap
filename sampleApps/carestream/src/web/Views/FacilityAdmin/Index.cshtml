@{
    Layout = null;
}

<div id="facility-list-container"
     hx-get="@Url.Action("Index", "FacilityAdmin")"
     hx-trigger="load once, refreshFacilityList from:body"
     hx-swap="innerHTML">
    <div class="flex justify-center items-center p-10">
        <span class="loading loading-lg loading-spinner"></span>
        <p class="ml-2">Loading facilities...</p>
    </div>
</div>