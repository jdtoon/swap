@using habits.Data.Models
@inject SignInManager<AppUser> SignInManager

<ul class="menu bg-base-200 w-full rounded-box">
    <li>
        <a class="@(ManageNavPages.IndexNavClass(ViewContext) == "active" ? "active" : "")"
           asp-page="./Index">
            <img src="~/svg/profile.svg" class="h-5 w-5" />
            Profile
        </a>
    </li>
    <li>
        <a class="@(ManageNavPages.EmailNavClass(ViewContext) == "active" ? "active" : "")"
           asp-page="./Email">
            <img src="~/svg/mail.svg" class="h-5 w-5" />
            Email
        </a>
    </li>
    <li>
        <a class="@(ManageNavPages.ChangePasswordNavClass(ViewContext) == "active" ? "active" : "")"
           asp-page="./ChangePassword">
            <img src="~/svg/password.svg" class="h-5 w-5" />
            Password
        </a>
    </li>
    <li>
        <a class="@(ManageNavPages.TwoFactorAuthenticationNavClass(ViewContext) == "active" ? "active" : "")"
           asp-page="./TwoFactorAuthentication">
            <img src="~/svg/verified.svg" class="h-5 w-5" />
            Two-factor authentication
        </a>
    </li>
    <li>
        <a class="@(ManageNavPages.PersonalDataNavClass(ViewContext) == "active" ? "active" : "")"
           asp-page="./PersonalData">
            <img src="~/svg/data.svg" class="h-5 w-5" />
            Personal data
        </a>
    </li>
</ul>