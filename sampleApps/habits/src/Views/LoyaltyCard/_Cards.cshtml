@model List<LoyaltyCardDto>

@if (Model.Count == 0)
{
    <div class="text-center text-gray-500">
        <p>No cards added yet</p>
    </div>
}
else
{
    foreach (var card in Model)
    {
        <partial name="_CardItem" model="card" />
    }
} 