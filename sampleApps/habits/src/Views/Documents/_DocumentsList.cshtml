@model PagedResult<DocumentDto>

@foreach (var document in Model.Data)
{
    <partial name="_DocumentItem" model="@document" />
}

@if (!Model.HasMore)
{
    <input id="table_end" type="hidden" value="0" />
}