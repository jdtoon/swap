@model double

<div class="progress-bar-container">
    <div class="progress-bar">
        <div class="progress-fill" style="width: @Model%"></div>
    </div>
    <span class="progress-text">@Model.ToString("F1")% Complete</span>
</div>
