@using Swap.Htmx
@{
    // Auto-suppress layout for HTMX requests when AutoSuppressLayout is enabled
    // This checks the flag set by SwapLayoutFilter
    Layout = Context.ShouldSuppressLayout() ? null : "_Layout";
}
