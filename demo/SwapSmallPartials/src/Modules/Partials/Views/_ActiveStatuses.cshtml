@model SwapSmallPartials.Modules.Partials.PartialsState

<div id="partial-active-statuses" class="card" style="background: var(--text-light); color: white; text-align: center; padding: var(--spacing-sm);">
    <div style="font-size: 0.75rem; opacity: 0.8; margin-bottom: var(--spacing-xs);">Active</div>
    <div style="font-size: 1.75rem; font-weight: 700;">@Model.ActiveStatuses / 5</div>
</div>
