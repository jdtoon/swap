@model SwapSmallPartials.Modules.Partials.PartialsState

<div id="partial-average" class="card" style="background: var(--secondary); color: white; text-align: center; padding: var(--spacing-sm);">
    <div style="font-size: 0.75rem; opacity: 0.8; margin-bottom: var(--spacing-xs);">Average</div>
    <div style="font-size: 1.75rem; font-weight: 700;">@Model.Average.ToString("F1")</div>
</div>
