@model SwapSmallPartials.Modules.Partials.PartialsState

<div id="partial-max" class="card" style="background: var(--primary); color: white; text-align: center; padding: var(--spacing-sm);">
    <div style="font-size: 0.75rem; opacity: 0.8; margin-bottom: var(--spacing-xs);">Max</div>
    <div style="font-size: 1.75rem; font-weight: 700;">@Model.Max</div>
</div>
