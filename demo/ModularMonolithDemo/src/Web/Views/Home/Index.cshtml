@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Home";
}

<div class="hero bg-base-200 rounded-lg p-8 mb-8">
    <div class="hero-content text-center">
        <div class="max-w-md">
            <h1 class="text-5xl font-bold">Welcome to Modular Monolith Demo!</h1>
            <p class="py-6">MVC + HTMX + DaisyUI. Modules own their UI via View Components.</p>
            <div class="badge badge-primary">HTMX Powered</div>
        </div>
    </div>
</div>

<div class="card bg-base-100 shadow-xl">
    <div class="card-body">
        <h2 class="card-title">Todos</h2>
        <form hx-post="/todos/ui/add" hx-on::after-request="this.reset()" class="flex gap-2 mb-4">
            <input type="text" name="title" placeholder="Add a todo..." class="input input-bordered flex-1" required />
            <button type="submit" class="btn btn-primary">Add</button>
        </form>
        @await Component.InvokeAsync("TodosList")
    </div>
</div>
