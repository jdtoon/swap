@* 
    This layout is used for HTMX requests - returns just the content without the full page wrapper.
    Used when hx-target="#main-content" swaps in pattern content.
*@
@{
    Layout = null;
}
@RenderBody()
