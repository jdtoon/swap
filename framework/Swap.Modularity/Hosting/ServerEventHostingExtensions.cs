namespace Swap.Modularity.Hosting;

// Intentionally left empty. The hosting extension for server events lives in Swap.Htmx.ServerEvents
// to avoid circular dependencies between framework projects.
internal static class ServerEventHostingExtensions { }
